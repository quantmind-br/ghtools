#!/bin/bash
# Mock fzf - select based on mode
if [[ " $* " == *" --multi "* ]]; then
    # Multi-select mode - return all lines
    cat
else
    # Single select - return first line
    head -n 1
fi
exit 0
